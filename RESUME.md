# dce_nik resume (2026-09-10). read this first in a new chat.

## the in vivo standard input (2026-09-10, user rule)
every method at k80: keep `spoke_masks/keep_f80match.npy` (v%10<8, 1368 of 1708 views), val `val_k80_m8.npy` (v%10==8), test `test_k80_m9.npy` (v%10==9); grasp v2 = `gv2_slice<Z>_n12_k80.npy`, grasp pro = `cs_slice<Z>_f80match.npy`; same binning, b1, crop, render, rois, rulers. the tofts vs patlak arms of section 10 were run at keep_f25 (488 spokes) and are NOT comparable to sub16 k80 or grasp k80; queue 005 reruns patlak / tofts / tofts8 at k80 (3 slices, 3 seeds, 3k steps, `results/tofts_vs_patlak/invivo_k80/`, eval `tofts_eval_invivo.py --spokes k80` -> `invivo_k80.md`, figures `--iv invivo_k80 --refs k80`), grasp k80 refs for 18/19 made by queue 007 (2026-09-11, after the upstream fix of probe 007); invivo_k80.md now complete on all three slices: on the model-free affine ruler grasp v2 k80 and grasp pro f80match lead every nik arm on 18/19 too (cortex 0.046 / 0.056 and 0.051 / 0.053 vs tofts8 0.069 / 0.059). still missing at k80 on 18/19: sub16 output-coil (outcoil_real.py, slice 21 only so far).

## how work runs now (luna decommissioned, helios has no claude)
laptop (claude, env `dce`) commits `jobs/queue/<n>.sh` (sbatch script) or `jobs/probe/<n>.sh` (inline, 60 s) -> `helios_agent.sh` (slurm defq, every 2 min) pulls, submits, commits `jobs/done/<n>`, `jobs/log/`, small results (json/md/png/csv) -> `python helios_fetch.py` on the laptop = git pull + jobs table + wandb (`dce_nik`) summaries in `results/_wandb/`. see `jobs/README.md`. training scripts log to wandb via `nik_wandb.py`. gpu quota: one 2g.24gb slice at a time (`QOSMaxGRESPerUser`), so arrays serialize.

## question
does a k-space inr (nik) beat compressed sensing (grasp pro, classic grasp v2) for radial dce-mri: images and contrast curves. phantom (xcat, truth) + in vivo (meas_p3_dce, slices 18/19/21, no truth).

## deliverables (the presentation)
- `results/xcat_physical_nomotion_nik_vs_grasp/NIK_vs_GRASPv2_combined_report.ipynb` (92 cells, 0 errors): nik vs grasp v2 + section 10 tofts vs patlak. lowercase, no dashes.
- `results/xcat_physical_nomotion_nik_vs_grasp/NIK_vs_CS_combined_report.ipynb` (61 cells): nik vs grasp pro (k12 cv-fair), sections 1 to 8 shared with the v2 notebook.
- `results/tofts_vs_patlak/REPORT.md`, `MANIFEST.md`, `REPRO.md`: tofts arm.
- `CONSOLIDATED_REPORT.md`, `report.md`: older text summaries, superseded where they disagree with the notebooks.

## figures (wandb, group tofts_figs, refreshed by every 00x_tofts job's eval stage)
https://wandb.ai/spshenov-university-of-amsterdam/dce_nik/groups/tofts_figs : per slice a run `figs_invivo_sl<Z>_<jid>` with panel (3 phases x arms + curves), roi overlay, all-seed curves vs model-free (raw scale), peak-frame difference, metric bars, test annuli. generator `tofts_figs_wandb.py`, queue `jobs/queue/003_tofts_figs.sh` (rename to rerun).
seen 2026-09-10 (sl21): tofts arms have the right curve shape but only ~60% of the model-free amplitude in the aorta peak and ~65% in the late medulla, patlak has the amplitude but the wrong medulla shape; the affine ruler hides the amplitude deficit, the raw-scale curves show it. sl18 (3k-step runs): the tissue amplitude deficit is nik-wide, patlak included (cortex and medulla plateaus at ~0.65 to 0.75 of model-free for all three nik arms, grasp v2 and pro on model-free); tofts arms additionally under-peak the aorta (0.57). hypothesis: early stopping on the static-dominated held-out k-space ruler (restored step 2000 in every run) selects under-fit dynamics; test = same runs without restore (final weights at 3k/8k) and compare raw-scale curves. relates to the nik_2901994 dynamics-metric flaw. roi overlay: cortex and medulla masks are 56 px each, the `liver` mask (1974 px) sits posterior over spine and muscle, not on the liver.

## established, with the number and where it lives
| fact | number | source |
|---|---|---|
| single fair setting vs grasp v2: 25 spf lam 0.25 on phantom (ideal-corner rule), carried to in vivo as nline 12; matched spokes (1720 / 1368) | | v2 nb sec 9, `v2_sweep/final_single_setting.json` |
| phantom images at that setting: tie | haarpsi 0.902 grasp vs 0.899 nik-sub16; sharpness_rel 1.004 vs 0.858 at equal bg noise; grasp psnr +1.6 db | `v2_sweep/image_metrics_panel.json` |
| phantom tissue curves: nik-free wins | cortex 0.011 vs 0.073, medulla 0.014 vs 0.051; aorta 0.081 vs 0.102 | `v2_sweep/frontier.json` |
| phantom bolus peak | nik +4 to +7%, grasp v2 -19% (lam 0.25), -7% (lam 0.02) | same |
| tuned grasp v2 (40 spf lam 0.02) beats nik on everything on the phantom | haarpsi 0.966, aorta 0.059 | v2 nb sec 9 |
| in vivo k80 pair (1368 spokes both): grasp v2 ahead on the model-free curve ruler | aorta 0.153 vs 0.234, cortex 0.084 vs 0.196 | `v2_sweep_invivo/v2_vs_nik_invivo_k80.json` |
| that ruler is biased: physical bound | aorta fwhm grasp v2 k80 53 s, nik 23 s, model-free 17 s, truth aif 12.5 s | `invivo_neutral_ruler.py` |
| nik vs grasp pro (k12 cv-fair) phantom | haarpsi 0.90 vs 0.87, psnr 37.4 vs 38.5, ssim 0.92 vs 0.95, aorta curve 0.08 vs 0.16 | cs nb sec 1, `l3_rebaseline*.csv` |
| in vivo temporal denoising real (cs oscillation = broadband noise) | nik 15 to 50x less oscillation | cs nb sec 2, `task_S*.json` |
| in vivo pk inter-slice consistency supports quantification | vp cov nik 2 to 5 vs cs 11 to 37 | `task2*.json` |
| output-coil beats input-coil on phantom, parity in vivo, 8x cheaper | psnr 37.2 vs 36.1 | cs nb sec 3 |
| tofts basis vs patlak, phantom no-motion | ssim 0.84 to 0.94, psnr +6 db, cortex 0.109 to 0.015, held-out k 13x; aorta dc offset +0.02 | `results/tofts_vs_patlak/phantom_nomotion.md` |
| tofts vs patlak, motion phantom | psnr +1.9 db, cortex 0.164 to 0.112, aorta worse | `phantom_motion.md` |
| fitted ext-kety parameter maps, phantom, 2026-09-12 (queue 020, `pk_maps_phantom.py`): spgr inversion with the true T10, DCE-NET curve_fit with the sim aif, 6138 enhancing voxels, same 5 of 7 spokes | kidney cortex truth ktrans 0.63 / ve 0.90 / vp 0.10: nik-free 0.61 / 0.87 / 0.12, nik-tofts 0.54 / 0.76 / 0.10, grasp pro K5 25spf 0.66 / 0.56 / 0.04, grasp 25spf 0.52 / 0.67 / 0.04 (fit floor on the truth signal 0.60 / 0.85 / 0.11). medulla truth 0.56 / 0.80 / 0.20: free 0.59 / 0.82 / 0.19, tofts 0.55 / 0.75 / 0.16, pro 0.79 / 0.65 / 0.12, grasp 0.58 / 0.66 / 0.07. liver-type tissues: every nik arm within 5%, grasp variants vp = 0. nik-patlak degenerate under an ext-kety fit (no washout atom). caveats: oracle T10 + aif, one seed, noise-free truth, vessels partial volume | `results/xcat_physical_nomotion_nik_vs_grasp/pk_maps/pk_maps.md`, `figures/pk_maps_phantom.png` |
| coefficient-domain ext-kety fit, phantom nik-tofts, 2026-09-13 (queue 029, `pk_coef_fit_phantom.py`): residual phi^T S(t; ke, dt, ve, vp, S0) - a against the path C coefficient maps (thC_best, rot180 + scale), exact spgr with the label T10, sim aif, 16 fixed atoms, no frames rendered; |thC @ Phi| reproduces rec_best to 1e-7 | agrees with the image-domain fit to 1 to 3% on every label (cortex ktrans 0.52 / 0.52, ve 0.73 / 0.73, vp 0.098 / 0.098; medulla 0.55 / 0.54, 0.75 / 0.74, 0.16 / 0.16); per-voxel median relative error vs truth ktrans 0.076 vs 0.081, ve 0.070 vs 0.074, vp 0.325 vs 0.326; 6138 voxels in 26 s. the residual tofts bias vs truth (cortex ktrans 0.52 vs 0.63, ve 0.73 vs 0.90) is in the coefficients themselves (the pk-arm amplitude deficit), not in the rendering or the fit | `results/xcat_physical_nomotion_nik_vs_grasp/pk_maps/pk_coef_tofts.md`, `figures/pk_coef_fit_phantom.png`; presentation 49 |
| span diagnostic, in vivo pk-arm amplitude deficit, 2026-09-15 (queue 030, `tofts_span_diag.py`, slices 21/18/19): model-free 31-spoke curves projected onto the fixed tofts atoms at ranks 3/5/8/12 vs what the trained arms produce on the same k80 input | the basis is NOT the limit: projection keeps 96 to 100% of the kidney first-pass peak and 99 to 100% of washout at rank >= 5 (rank 3 = patlak span: 98% peak, 90% washout); the trained arms lose it anyway: cortex peak / washout NIK-tofts (r12) 0.40 / 0.62, tofts8 0.44 / 0.66, patlak 0.71 / 0.82; aorta tofts 0.64 vs patlak 0.99 although both have the aif as atom 1; static liver gets 1.3 to 1.6x the model-free enhancement in the tofts arms. more atoms -> less kidney amplitude and more spurious dynamics in static tissue -> optimization / regularization / early-stop problem, not representation. basis aif matches the model-free aorta (ttp 63.8 vs 63.2 s, plateau 0.23 vs 0.23) | `results/tofts_vs_patlak/span_diag_sl{21,18,19}.{md,json}`, `figures/span_diag_sl*.png` |
| approved anatomical rois, 2026-09-15 (queue 034/035 proposal, 036 rerun; `roi_propose_invivo.py` v2 wired into `consolidated.slice_ctx`, `ROIS_OLD=1` restores the crescents): kidney = late-enhancement blobs from the model-free series, both kidneys, 1 px erosion; cortex / medulla by the smoothed first-pass-to-late ratio, medulla islands < 15 px dropped; aorta unchanged; old rois were a 112 px crescent on one kidney rim and a posterior 'liver' over spine/muscle | slice 21 / 18 / 19: kidney 894 / 1122 / 930 px, cortex 473 / 643 / 496, medulla 421 / 479 / 434; model-free cortex ttp 71 s plateau 0.62, medulla ttp 230 to 260 s plateau 0.9 (physiological). span diagnostic with the new rois: projection rank 8 keeps 0.95 to 0.99 of both kidney peaks and 1.00 of washout; trained tofts r12 cortex 0.48 to 0.53 / 0.67 to 0.71, tofts8 0.54 to 0.57 / 0.69 to 0.74, patlak 0.81 to 0.88 / 0.84 to 0.85 (medulla tofts 0.61 to 0.67 / 0.62 to 0.75). deficit persists, so it is not the crescent's partial volume; story panels 45 / 48 regenerated with contours | `results/realdata_nik_vs_cs_figures/{rois_proposed.md, rois_proposed_sl*.npz, roi_check_k80.md, figures/roi_proposed_sl*.png, figures/roi_check_k80_sl*.png}`, `results/tofts_vs_patlak/span_diag_sl*.md` |
| amplitude-vs-step test, 2026-09-16 (queues 031/032/038, `tofts_amp_track.py`, tofts8 sl21 k80 seed 0, 40k steps, no restore, snapshot every 2000; trainer flags `--snapshot-every`, `--no-restore`; phantom `xph_train --tag-suffix/--dcf-power`, `XPH_ENV`): cortex peak / washout vs model-free at step 2000 -> 40000 | base (lr 1e-5, wd 3e-3, unit-norm atoms) 0.53 / 0.74 -> 0.70 / 0.93; wd0 0.56 / 0.82 -> 0.85 / 1.00; atomscale (unit-rms atoms, `basis_sl21_r8_rms1.npz`) 0.78 / 0.92 -> 0.87 / 1.01 (medulla 0.95 / 1.01, aorta 0.92 / 1.00); lr1e-4 0.28 / 0.43 -> 0.41 / 0.57 (static wrong, liver 1.5 to 1.9x); env0 0.27 / 0.42 -> 0.53 / 0.75; dcf1 0.44 / 0.61 -> 0.53 / 0.72. VERDICT: the deficit = atom scale (18x smaller than patlak's) + weight decay + the step-2000 restore; the held-out k-space mse RISES monotonically from step 2000 in every variant while the curves improve (it selects the worst dynamics; val ruler is static/noise dominated), so no early stop for pk arms. loss weighting is not the cause: env0 breaks everything (phantom aorta +0.13, imgNRMSE 0.098), dcf1 changes nothing. phantom aorta +0.02 excess unchanged by dcf1 (+0.018) -> still open, shared with nik-free, not weighting | `results/tofts_vs_patlak/amp_track_sl21.{md,json}`, `figures/amp_track_sl21.png`, `results/xcat_physical_nomotion_nik_vs_grasp/aorta_offset_lossw.md` |
| production rerun of the pk arms, 2026-09-17 (queue 040, `invivo_k80_rms1/`, `basis_sl<Z>{,_r8}_rms1.npz` from `basis_rms1.py`, 10k steps, `--no-restore`, 3 seeds, slices 18/19/21, eval `tofts_eval_invivo.py --iv-dir invivo_k80_rms1 --basis-suffix _rms1` with peak/washout ratio columns): THE IN VIVO PK-ARM STANDARD FROM NOW ON | slice 21 cortex peak / washout vs model-free: patlak 0.93 / 0.86, tofts (r12) 0.71 / 0.95, tofts8 0.81 / 0.97 (grasp 0.77 / 1.05, grasp pro 0.64 / 0.92); medulla patlak 0.69 / 0.91, tofts 0.91 / 0.97, tofts8 0.92 / 0.99; aorta all pk arms 0.94 to 1.02 (grasp 0.71, grasp pro 0.23). curve NRMSE vs model-free (one global scale) sl21: cortex tofts8 0.059 < grasp 0.074 < sub16 0.115 < free 0.150 < grasp pro 0.218 < patlak 0.289; medulla tofts8 0.043 < free 0.062 < grasp 0.083 < sub16 0.091 < grasp pro 0.130 < patlak 0.234; aorta patlak 0.069 < tofts8 0.103 < grasp 0.153. slices 18/19 same ordering (tofts8 cortex 0.84 / 0.98, 0.88 / 0.98). affine ruler: tofts8 cortex 0.038 / 0.043 / 0.036, medulla 0.027 / 0.024 / 0.024 (best of all methods on every slice). test kNMSE at 10k: patlak 0.244 / 0.275 / 0.274, tofts8 0.258 / 0.288 / 0.297, tofts 0.269 / 0.332 / 0.304 (the k-space ruler ranks the arms opposite to the curves; it is noise-fit dominated, do not select on it). wall 38 min per run on a 2g slice. old invivo_k80/ (unit-norm atoms, 3k, restore) kept for the before/after | `results/tofts_vs_patlak/invivo_k80_rms1.{md,json}`, `span_diag_sl*_rms1.md`, `figures/span_diag_sl*_rms1.png`, story panels `story_invivo_k80_sl21*_rms1{,_t8}.png`, wandb group figs_invivo_k80_rms1; presentation 45 / 48 refreshed, 50 / 51 added |
| image quality vs curve fidelity, 2026-09-17 (queues 043/044, `tofts_iq_track.py`: haarpsi / ssim / psnr vs grasp-pro all-spoke anatomy at 90 and 300 s, air energy = streaks + noise, temporal noise in the static roi; tofts8 rms1 sl21 12k steps, snapshots every 1000, wd 3e-3 vs 1e-2) | final recons at 90 s: grasp haarpsi 0.740 / ssim 0.885 / airE 0.134; tofts8 new (10k) 0.716 / 0.851 / 0.180 (old 3k restore 0.743 / 0.903 / 0.129 but cortex nrmse 0.302); at 300 s tofts8 0.815 > grasp 0.785. along training (wd 3e-3): haarpsi 0.58 (1k) -> 0.71 (3k) -> 0.73 (8k+), airE rises monotonically 0.11 -> 0.155 (3k) -> 0.18 (10k), cortex nrmse 0.15 -> 0.086 (3k) -> 0.056 (8k). NO step gives grasp-like airE with good curves: the noise fit and the dynamics grow together, so the step count is not the knob (user's point). wd 1e-2: consistently but modestly better at every step (8k: haarpsi 0.736, ssim 0.862, airE 0.169, cortex peak 0.88, nrmse 0.049) | `results/tofts_vs_patlak/iq_track_sl21{,_trade}.md`, `figures/iq_track_sl21_trade.png`, `figures/iq_zoom_sl21.png`; next: 045 (cosine, cosine + wd 1e-2, coefficient-map huber tv 0.03 / 0.3, seed average), memory project_dce_nik_noise_knobs |
| ARM INCONSISTENCY FLAGGED 2026-09-17: the in vivo NIK-sub16 in every k80 panel / table so far is the OUTPUT-coil model from `outcoil_real.py` (own trainer, hidden 768, no pca warm start, best-val restore at 30k), while NIK-free / patlak / tofts are INPUT-coil models from `train_grasp_nik.py`. The output-coil model is known to under-read enhancement amplitude in vivo (memory project_dce_nik_outcoil). User rule: every non-studied difference between compared arms must be flagged and approved. Fix in flight (queue 051): sub16 rerun as input-coil with the pk-arm trainer and protocol (wire_ff_subspace rank 16, pca warm start, 10k, no restore, wd 3e-3, k80, 3 seeds, slices 18/19/21) into `invivo_k80_rms1/sub16_*`; plus tofts8 with the new `--coil-mode output` head (nik_model coil_mode, shared backbone, 2RC outputs) into `invivo_k80_oc/` as the output-coil test on the pk arm. Story panels now state the coil mode in the note and label a mismatched sub16 explicitly | pending | `jobs/queue/051_sub16_input_tofts8_output.sh`, eval `invivo_k80_rms1.md` (arms + sub16), `invivo_k80_oc.md` |
| regularization ideas, 2026-09-17 (queue 045, tofts8 rms1 sl21 12k, snapshots; `iq_track_sl21_ideas.md`): cosine lr to 1e-7, cosine + wd 1e-2, 3-seed magnitude average of the 040 tofts8; the two coefficient-map tv runs died at the first tv render (CUDA OOM, 384^2 x 8 coils with grad) and are requeued chunked + checkpointed (queue 053) | cosine (wd 3e-3) at 12k: haarpsi 0.704, airE 0.205, cortex peak 0.91 / nrmse 0.055: a converged endpoint fits MORE noise, so the schedule alone is not the knob. cosine + wd 1e-2 at 12k: 0.727 / 0.187 / peak 0.93 / nrmse 0.052 (044 constant-lr wd 1e-2 at 8k: 0.736 / 0.169 / 0.88 / 0.049). seed average avg3: haarpsi 0.748 (> grasp 0.740), flat noise 0.237, highpass 0.928, cortex nrmse 0.065, medulla 0.039, but airE 0.184 = UNCHANGED: the streak texture is the same in every seed, i.e. deterministic aliasing the model reproduces, not noise fitting -> only a k-space consistency prior (PISCO / support) can remove it; averaging and weight decay cannot | `results/tofts_vs_patlak/iq_track_sl21_ideas.md`, `amp_track_sl21.md`, `invivo_k80_rms1/tofts8_sl21_avg3/` |
| model-free reference under-reads the first-pass peak, 2026-09-17 (queue 054, `mf_peak_check.py`: the 31-spoke sliding-window nufft regridded at 21 / 15 / 11 / 7 spokes, per-spoke normalized, approved rois) | cortex peak relative to the 31-spoke value: sl21 1.01 / 1.05 / 1.07 / 1.13, sl18 1.01 / 1.09 / 1.12 / 1.18 (7-spoke windows noisy); aorta peak / washout 4.4 -> 4.9 (+11%). the reference's 6.8 s window clips the kidney first pass by roughly 7 to 13%. consequence: a nik cortex peak ratio of 0.88 to 0.93 vs the reference (tofts8 wd 1e-2) is still BELOW the true peak (about 0.80 to 0.87 of it), grasp at 0.77 is about 0.70 of it; the wd 1e-2 gain is real amplitude recovery, not an overshoot. every peak ratio quoted against model-free carries this bias; washout ratios do not | `results/tofts_vs_patlak/mf_peak_check_sl{21,18,19}.{md,json}` (md table before the per-spoke fix is scale-confounded, use the json ratios or rerun) |
| coil-mode consistency rerun, 2026-09-18 (queue 051): NIK-sub16 as INPUT-coil with the pk-arm trainer and protocol (wire_ff_subspace r16, pca warm start, 10k, no restore, wd 3e-3), and NIK-tofts8 with the new OUTPUT-coil head (`--coil-mode output`), 3 seeds, slices 18/19/21, into `invivo_k80_rms1/sub16_*` and `invivo_k80_oc/tofts8_*`; panels regenerated with the coil mode in the note (45 / 48s replaced) | sub16 input-coil sl21: cortex 0.79 / 0.94, medulla 0.91 / 0.88, aorta peak 0.59, affine cortex 0.067 / medulla 0.080, curves visibly oscillating (nrmse aorta 0.33, cortex 0.13, medulla 0.15); held-out k-space TEST kNMSE 0.053 vs 0.26 for tofts8 (k-centre annulus 0.049 vs 0.23, outer 1.26 vs 1.60): the learned 16-atom basis reproduces temporal content of the held-out spokes that no tofts atom can (respiration and other non-kinetic dynamics; the 31-spoke reference averages them out), which is exactly why the k-space ruler ranks the pk arms last and why the pk basis is the right model for kinetics and the wrong one for everything else. tofts8 output-coil: cortex 0.84 / 0.97 (sl21), 0.87 / 0.98 (18), 0.90 / 0.98 (19); affine cortex 0.035 / 0.039 / 0.035, medulla 0.030 / 0.024 / 0.027; aorta peak 1.03 +- 0.02: equal to or slightly better than input-coil tofts8 on every slice (input: 0.81 / 0.84 / 0.88 cortex peak, affine 0.038 / 0.043 / 0.036), same held-out kNMSE (0.258 vs 0.258). output-coil is a valid, slightly better and cheaper parameterization for the pk arm; no amplitude penalty here (the old outcoil_real sub16 under-read because of its own protocol) | `results/tofts_vs_patlak/invivo_k80_rms1.md` (sub16 column), `invivo_k80_oc.md`, `figures/story_invivo_k80_sl21*_rms1_t8.png` |
| k-space priors and tv, 2026-09-18 (queues 053 / 055 / 057, tofts8 rms1 sl21 12k): coefficient-map huber tv 0.03 / 0.3 (chunked render), support prior 0.3 / 3.0, pisco 0.1 / 1.0 | tv 0.3 at 12k: haarpsi 0.746 (> grasp 0.740), airE 0.169 (base 0.177, grasp 0.134), cortex peak 0.84, nrmse 0.068 (base 0.068): the only variant so far that raises haarpsi AND lowers airE with unchanged curves, modest. support prior: no effect on the rendered airE (0.178 / 0.179) although the penalty itself went to 4.5e-6 and the mask orientation check found inside-energy fractions ~0.79 for every orientation (suspicious: geometry of the prior render vs the production render under investigation, queue 058). pisco: OOM at start (74k evaluations with grad); per-coil checkpointed rerun queued (057) | `results/tofts_vs_patlak/iq_track_sl21_{ideas2,kspace}.md`, `figures/support_diag_sl21.png` (pending) |
| k-space priors, diagnostics 2026-09-18 (queues 057 / 058): support prior v1 inert because it took the MEAN energy over the whole 384 field outside the mask (ratio 3e-4 at base already; the streaks are 25% of the energy inside the 192 crop but a tiny mean over the empty margin); mask orientation rot180 is the right one (inside fraction 0.91 vs 0.79 for id). pisco v1 inert: with own-coil neighbours in the stencil the relation is satisfied to 0.25% by any smooth k-space (residual 0.0025 at the end, airE 0.177 / 0.175 = base), so it never saw the aliasing | v2 queued (059): support = sum of energy in the air ring inside the crop / sum inside the body, weights 1 and 10; pisco cross-coil only (coil c predicted from the other coils, so only coil diversity can explain the target), weights 0.1 and 1 | `figures/support_diag_sl21.png`, `iq_track_sl21_kspace.md` |
| k-space priors v2, 2026-09-18 (queue 059, `iq_track_sl21_kspace2.md`): support prior v2 (air-ring energy in the crop / body energy, weights 1 and 10), pisco cross-coil (0.1 and 1) | SUPPORT v2 WORKS: air energy 0.133 at 12k (grasp 0.134, base 0.177), HaarPSI 0.730 (9k: 0.736 / 0.132), curves unchanged (cortex nrmse 0.074 / medulla 0.046 at 12k; 0.063 / 0.044 at 9k; peak 0.81 to 0.83); weight 10 the same. pisco cross-coil: inert again (air 0.176 / 0.171, residual 1.2%). the streak excess of the pk arm is removed by the support prior at unchanged curves: candidate production protocol = wd 1e-2 + support 1 | `figures/claims_regularization_sl21.png` (53), `FINDINGS.md` |
| RULES 2026-09-18 (user): (1) SELECTION METRIC: held-out k-space is NOT a selection criterion for in vivo arms. Under free breathing it rewards reproducing respiration and other non-kinetic content that the 6.8 s reference averages away (sub16 5x lower held-out error than tofts8 by exactly that). There is no clean truth-free selection metric in vivo under motion; model selection moves to the motion phantom. The pk arms' higher held-out error is the basis correctly refusing to fit respiration, not a deficit. (2) PEAK CORRECTION: the 31-spoke reference clips the first pass by 7 to 13%; every in vivo peak comparison states this, and the curve tables carry a corrected-reference block (`add_peak_correction.py`, factor = 11-spoke / 31-spoke peak per slice and roi: cortex 1.07 / 1.12 / 1.09, medulla 1.04 / 1.07 / 1.06, aorta 1.10 / 1.09 / 1.09 for sl21 / 18 / 19). (3) SUPPORT PRIOR: not reported until the identical inside fractions of the in-training orientation check were explained. Explained by 058: that check ran at step 32 on an untrained model whose energy is nearly uniform over the field, so every orientation returned the mask area fraction (0.79); on trained models rot180 gives 0.91 vs 0.79 for id, and the prior render matches the production render (energy in the crop 0.949, 25% of it outside the body, consistent with airE 0.18). The pick happened to be right; orientation is now explicit (`--support-orient rot180`). REPORTING: differences under 0.01 in HaarPSI stay inside noise and are not headlined; tv 0.3 is a modest real air-energy gain; pisco is reported on both axes plus air energy against the input-coil protocol arm with the reference correction | | `FINDINGS.md`, `invivo_k80_rms1.md`, `invivo_k80_oc.md` (corrected block) |
| PRODUCTION RUN queued 2026-09-21 (queue 063, 39 gpu tasks + chained eval; user decisions after the audit): support prior 1 on ALL coefficient-map arms (tofts8 both coil modes, patlak, sub16), NIK-free cannot take it (no coefficient maps) and the panel note says so; NIK-free rerun under the standard protocol on 18/19/21 (val spokes only, 10k, no restore, wd 1e-2) because the old results_sl21_k80 model early-stopped on val+test with restore-best at 40k, so the earlier oscillation / denoising comparison must be redone on the rerun before it stays in any deliverable; mask dilation 6 px = about 12 mm at 2 mm pixels, above the anterior-wall breathing excursion, instead of the 4 px = 8 mm test value; matched base (wd 1e-2, no prior) for tofts8 input-coil 3x3 and output-coil 1 seed per slice so the prior's gain is attributable. Record: per-weight wd on the 8x larger output head = 8x aggregate shrinkage on that head, flag if the coil modes diverge. Audit before queueing: input data identical across all k80 arms (masks disjoint, union 1708, normalizer on kept samples), b1 never in training for any nik arm, same backbone (hidden 512, depth 12), coil modes cleanly separated, prior render geometry = production render frame (rot180 explained by ifft2c_mri's fft sign) | pending | dirs `invivo_prod`, `invivo_prod_oc`, `invivo_prod_base`; tables `invivo_prod{,_oc,_base}.md` with corrected peaks; `figures/prod_sl*.png`, story panels `_prod` |
| PRODUCTION RESULTS 2026-09-22 (queue 063 done, 39 runs, eval chained; `invivo_prod{,_oc,_base}.md` with corrected peaks, `iq_track_sl*_prod_sl*.md`, `figures/prod_sl*.png`, story `_prod`): | TOFTS8 INPUT-COIL + PRIOR (3 seeds, sl21/18/19): cortex peak raw 0.85 / 0.90 / 0.90 (corrected 0.79 / 0.80 / 0.82), washout 1.00 / 1.00 / 0.99, medulla peak 0.96 / 0.97 / 0.97, aorta peak 1.01 / 1.05 / 1.00; cortex nrmse 0.055 / 0.060 / 0.055, medulla 0.042 / 0.051 / 0.043 (grasp 0.075 / 0.077 / 0.094 and 0.079 / 0.060 / 0.073); air energy 0.138 / 0.100 / 0.088 vs matched base without prior 0.172 / 0.132 / 0.108 vs grasp 0.134 / 0.124 / 0.112: the prior brings the streak energy to or below grasp on every slice with the curves unchanged (base cortex nrmse 0.053 / 0.062 / 0.053); HaarPSI 0.732 / 0.722 / 0.721 vs base 0.722 / 0.726 / 0.732 (noise) vs grasp 0.740 / 0.768 / 0.791 (grasp sharper on 18/19 by 0.05 to 0.07, real). OUTPUT-COIL + PRIOR: HaarPSI 0.750 / 0.762 / 0.769 (above input-coil by 0.02 to 0.05 on every slice, real), air 0.139 / 0.108 / 0.100, curves slightly worse than input-coil (cortex nrmse 0.068 / 0.069 / 0.072, medulla washout 0.97 / 0.93 / 0.90 vs 1.02 / 0.97 / 0.96): coil modes diverge mildly in the direction the 8x aggregate head shrinkage predicts (smoother maps, slightly damped washout); no amplitude penalty at the peak. PATLAK + PRIOR: sharpest pk arm (HaarPSI 0.786 / 0.775 / 0.771, air 0.128 / 0.094 / 0.090) but curve shape wrong as always (cortex nrmse 0.24 / 0.29 / 0.33, no washout). SUB16 + PRIOR: sharp (0.808 / 0.791 / 0.780) but curves worse than before (cortex nrmse 0.35 / 0.36 / 0.27, peak 0.59 to 0.65) with the same oscillation: the prior + wd 1e-2 did not damp the non-kinetic content. NIK-FREE standard protocol (10k, val-only stop, wd 1e-2): HaarPSI 0.780 / 0.793 / 0.788, air 0.165 / 0.138 / 0.130, cortex nrmse 0.13 / 0.14 / 0.15, aorta peak 0.69 / 0.64 / 0.71: same picture as the old model (sharp, dynamics clipped), so the earlier oscillation / denoising comparison holds qualitatively but must be re-quoted from these runs. VERDICT: tofts8 input-coil + prior is the in vivo pk-arm protocol; on the same k80 input it beats grasp on every kidney curve on all three slices at grasp-level streak energy, and trails grasp in HaarPSI on 18/19. Reference clips the peak by 7 to 13% (stated). Held-out kNMSE not used for selection | `results/tofts_vs_patlak/invivo_prod*.md`, `iq_track_sl{21,18,19}_prod_sl*.md`, `figures/prod_sl*.png` (presentation 55), `story_invivo_k80_sl21_prod.png` (45) |
| NEW DATASET 2026-09-23/24: p8 (meas_p8_dce, same protocol as p3) is a TRUNCATED copy (twix parse stops 4.2 MB before the header's end, 1478 of 1723 views, last 53 s missing; file size sits just under the 4 GiB FAT32 limit) and is NOT used. Replacement: meas_topqmri_p14.dat (complete, 5.46 GB): base 256 (nx 512, bas 256), 24 coils, 2157 views after the cut, TA 390 s, TR 5.95 ms, flip 15, 30 slab-cropped slices, protocol DCE_t1_vibe-grasp_fs_TRA_adapt_6_test_timing; slab = heart to liver / spleen, kidneys not in the slab. User decisions: liver + spleen + aorta rois, slices 21 / 24 / 27. Pipeline made dataset-aware: `dsp.py` (DCE_DS=p3|p8|p14: REF, GV, GP, NUF, STEP2, AIF, ROIS, SUPPORT, BASIS, KEEP/VAL/TEST, TA/nx/bas/ntviews from shared.npz, tissue roi names T1/T2 = cortex/medulla on p3, liver/spleen on p14, STATIC), `spoke_masks_build.py` (keep v%10<8 / val 8 / test 9 from the view count), `roi_propose_liver.py`, survey script `grasp_pro_py/survey_slices.py`; grasp_v2_real.py and cs_nikmatch.py take DCE_DS too. p3 regression: every default path unchanged, slice_ctx(21) and all chain imports pass. Queue 067 = p14 preparation (precompute 21/24/27, masks, rulers, model-free series, aif gate, liver / spleen roi proposal for approval) | pending | `dsp.py`, `results/realdata_nik_vs_cs_figures/figures/survey_p{8,14}.png`, `roi_proposed_p14_sl*.png` (after 067) |
| p14 preparation done 2026-09-25: slices 21 / 24 / 27 precomputed (results_ref_p14, 20 min per slice on 16 cores; the sequential 067 was cancelled and redone as a per-slice array 070/071; two array tasks first failed with 'raw file not found' on worker03/08, the rerun on other nodes succeeded: node-dependent visibility of dce_data, keep in mind), spoke masks (keep 1727 / val 215 / test 215 of 2157 views), nufft rulers, model-free series, aif gate. ROIs: the automatic liver / spleen rules were not robust (organ heuristics landed on the wrong regions, the aorta was never found because in this slab it is bright BEFORE contrast, inflow of unsaturated blood, so it barely enhances relative to baseline; the body mask from the p3 quantile rule collapsed on slice 24 -> robust body mask for non-p3). Final ROIs = MANUAL: the user painted closed outlines (red liver, green spleen, blue aorta) on 2x templates of the model-free 120 s frame (`results/rois_manual/template_p14_sl<Z>_t120.png`), ingested by `roi_manual_ingest.py` (fill, downsample, erode 2 px) -> `rois_proposed_p14_sl<Z>.npz`; liver 7295 / 7762 / 7446 px, spleen 3243 / 3422 / 3667, aorta 217 / 198 / 176; no static roi (temporal-noise readout falls back to the body). Overlays `figures/roi_manual_p14_sl*.png`. Caveat for every p14 result: the aorta AIF is inflow-affected (small enhancement over a bright baseline, first-pass shape clean) | approved by the user 2026-09-25 | queue 068 = aif gate with the manual aorta, basis r8 + rms1, support masks d6 (512 grid), grasp-pro f80match, grasp v2 n12 k80 |
| quick in vivo ext-kety maps at k80, 2026-09-13 (queue 022, `pk_maps_invivo.py`): literature T10 (cortex 1142, medulla 1545, blood 1650, other 1000 ms), TR 4.66 / flip 18 from the twix header, aif = model-free aorta inverted (peak 8.6 mM at 63 s, cosine4 fit nrmse 0.10), one aif for every method, DCE-NET fit on all body voxels | slice 21 cortex ktrans / ve / vp: nik-free 0.20 / 0.30 / 0.041, nik-sub16 0.23 / 0.35 / 0.051, grasp n12 k80 0.32 / 0.49 / 0.067, grasp pro f80match 0.14 / 0.36 / 0.027, nik-tofts 0.089 / 0.17 / 0.012, tofts8 0.10 / 0.18 / 0.021, patlak degenerate (ve 1). literature healthy cortex ~0.2 to 0.4 / 0.2 to 0.4 / 0.05 to 0.15: nik-free, sub16 and grasp plausible, the pk arms at about half (their amplitude deficit), grasp pro in between. across slices 18 / 19 / 21 cortex ktrans: grasp 0.30 / 0.30 / 0.32, tofts 0.074 / 0.062 / 0.089, grasp pro 0.20 / 0.24 / 0.14. no truth: consistency and plausibility only | `results/realdata_nik_vs_cs_figures/pk_maps/pk_invivo_k80.md`, `figures/pk_maps_invivo_k80_sl*.png` |
| tofts vs patlak, in vivo | kidney curves better, cortex/medulla identity broken (0.998 to 0.7 to 0.9), aorta peak 0.5 vs 0.9 (patlak by construction), held-out k 8% worse globally (k centre), better at |k|>0.19 | `invivo.md` |
| tofts rank 8 (forced) vs rank 12 (rule), in vivo sl21, 3 seeds, 2026-09-10 | test knmse 0.294 vs 0.311 (patlak 0.289): k-centre gap mostly closed (annulus 0 0.276 vs 0.294, patlak 0.270), best of the three on all 15 outer annuli; cortex/medulla affine 0.117/0.061 vs 0.139/0.075 (patlak 0.155/0.139); aorta affine 0.145 vs 0.178 (patlak 0.053, grasp v2 0.149); late corr -0.22 (model-free sign). basis rule not met at 8 (first-pass err 0.0285 > 0.01) | `results/tofts_vs_patlak/invivo_r8.md`, `jobs/queue/001_tofts_sl21_rank8.sh`, wandb `gnik_tofts8_sl21_s*` |
| tofts rank 8 at 3k steps, slices 18/19, 3 seeds each (queue 002, 2026-09-10) | test knmse 0.3265 / 0.3289 = patlak 0.3271 / 0.3291 (rank 12: 0.349 / 0.356), k-centre annulus 0.297 / 0.288 = patlak; cortex affine 0.082 / 0.088 (r12 0.095 / 0.117, patlak 0.152), medulla 0.056 / 0.050 (r12 0.062 / 0.069, patlak 0.139); aorta affine 0.156 / 0.156 (patlak 0.060 / 0.037), peak ratio 0.57 / 0.56; wall 990 s vs 8000 s, same quality -> 3k steps is the in vivo protocol from now on | `results/tofts_vs_patlak/invivo_r8.md`, `jobs/queue/002_tofts_sl18_19_rank8_3k.sh`, wandb `gnik_tofts8_sl1*` |
| k80 standard, pk arms (queue 005, 2026-09-10): patlak / tofts r12 / tofts r8, 3 slices x 3 seeds, 3k steps | held-out test knmse 18: 0.280 / 0.277 / 0.278, 19: 0.282 / 0.276 / 0.277, 21: 0.249 / 0.250 / 0.255: at k80 rank 12 no longer overfits the k-centre (the rank 8 gain was an f25 effect); kidney affine: tofts8 best everywhere (cortex 0.069 / 0.059 / 0.084, medulla 0.049 / 0.040 / 0.047) vs r12 (0.083 / 0.074 / 0.108) vs patlak (0.15 / 0.14); grasp v2 k80 on 21 better still (0.053 / 0.035); aorta peak ratio tofts8 0.72 to 0.80, r12 0.65, patlak 0.97, grasp v2 0.71, grasp pro 0.23. raw-scale curves: nik tissue amplitude deficit persists at k80 (0.55 to 0.65 of model-free), so it is structural, not spoke count. probe 006: not a gain error: nik static intensity in aorta/cortex/medulla is 1.6 to 2.0x model-free (grasp v2 1.05 to 1.15) while the enhancement is 0.6 to 0.85x, i.e. relative enhancement ~1/3 of model-free; the temporal component is under-fit relative to the static one (uniformly weighted k-space loss, dcf off) |
| correction (story panel, k80, 2026-09-11): the tissue amplitude deficit is a pk-basis effect, not nik-wide: on the same k80 input nik-free and nik-sub16 track the model-free cortex/medulla plateau (like grasp v2), patlak and tofts sit at ~0.55 to 0.75 of it. the earlier "all nik arms" statement was based on the three pk arms only | `results/realdata_nik_vs_cs_figures/figures/story_invivo_k80_sl21.png` | `results/tofts_vs_patlak/invivo_k80.md`, wandb `figs_invivo_k80_sl*` |
| every in vivo tofts_vs_patlak run (patlak, tofts r12, r8; slices 18/19/21; 3 seeds) restores the step 2000 weights | held-out mse rises monotonically from step 1000 (sl21 tofts 0.56 at 2k to 0.75 at 40k, patlak 0.62 to 0.88) while train falls; warmup 2000 pins the restored step; the reported wall_s 8000 is the 40k run, the evaluated model is ~400 s of training | `jobs/log/probe_002_restored_best.out` |

## retracted, do not resurrect
- "cs wins at every spoke fraction": pca basis leak (basis from all spokes). fair: level, nik ahead at 50%.
- "grasp v2 aif damping is structural": lam artifact; lam 0.02 fixes it.
- "grasp v2 6.7x worse than pro": 5 spf operating-point artifact (v2 nb section 1 still shows it, known, left as is).
- "nik and grasp use different b1": noise probe outside the body; in-body diff 1e-4.
- "nik overshoots cortex" and "cs over-reads cortex 12%": per-roi peak-normalization artifacts; use raw + affine.
- "render is a lossy readout, feed spokes to mcnufft": wrong, non-realizability is not readout loss.
- "output-coil under-reads bolus 14%": baseline-norm artifact; calibrated equal.
- "aif time grid mismatch 0.44 s": grids only; all axes share view_time.

## tested, negative, do not repeat
temporal tv on atoms (phi_tv) and on k-space (ktv21, l2,1): null. sense-forward a (image inr + b1): works, loses to sub16. gaussian activation: bad at every w0. rank knob: saturates (realized rank ~5). dcf_power 1.0: fits noise. per-frame data consistency: strictly hurts. binning-free pk differentiator: not real at 5 to 15 spf. spirit on nik: inert. post-hoc tv: no.

## open
- neutral in-vivo ruler: held-out spokes for cs need complex-valued grasp saves (`grasp_v2_real.py` saves magnitude). the one thing that would settle in-vivo curves.
- sense-b (k-space model + b1 factorization in k-space): only untested item on the image axis. kernel study done (r=2 taps capture 58%, factorization residual 8 db), training not run.
- tofts in vivo: rank 8 closes the k-centre gap on all three slices (2026-09-10). open: the amplitude deficit of the tofts arms (aorta peak ~0.57 of model-free, late medulla ~0.65, see figures), the basis rule threshold (0.01 rejects rank 8), rank 5 (patlak span + 2) as the next point on the curve, and the roi masks (cortex/medulla 56 px, `liver` mask not liver).
- v2 notebook section 1 still at 5 spf (~3 h to repoint).
- fwhm bound on slices 18/19/20 for grasp v2.

## pipeline facts that bite
- coords: in vivo x in [-1,1] (traj_norm*2), phantom kx in [-0.5,0.5] (`_dataset` doubles). radius bins: check the range.
- render: 2x oversample + rot180 with 1px roll (even grid) + support radius 1.0. three render bugs were found this way; any new image-vs-truth number goes through `recon_asserts.check_recon`.
- `--spoke-keep-file` alone disables early stopping and lr schedule; add `--keep-heldout` or `--spoke-heldout-file`.
- in vivo early stop: `--warmup-steps 2000` means the restored model is the step 2000 one in every run so far; the 40k steps and the plateau scheduler never matter. any in vivo number is a 2000 step model.
- agent: logs of running jobs must not be committed (a later pull rewrites the inode, slurm keeps writing to the old one); the agent defers by script name. changing `helios_agent.sh` needs `scancel -n helios-agent` + `sbatch` (slurm copies the script at submit).
- in-vivo run variants: `results_sl21_k80` is the fair one; `results_full_sl21_matched` had no early stop; `results_spoke_full_slice21` is random 70%.
- metrics: masked haarpsi/ssim, same window-averaged truth for every method, one global ls scale, roi mean on the same masks (`xph_common.rois`, `consolidated.slice_ctx`). curves vs model-free: raw + affine.
- time axis in vivo: everything derives from `view_time = v/1709` times 375 s.
- `srun --jobid=<session>` runs 2 tasks; add `--ntasks=1`. system python3 is 3.6; use the torch29 python.
- gpu: luna-01 only; mig 1g (phantom, 5 gb) / 4g (in vivo, 12 gb); 2g slices often unschedulable when 1g are full.

## reproduce
- v2 notebook: `build_gv2_nb.py` clones the cs notebook and patches sections 9; section 10 and the prose rewrite of 2026-09-08 were applied to the ipynb directly (rebuild from the builder would lose them; patch the builder before rebuilding). 2026-09-10: rank 8 / early stop / inline figure cells (section 10), a summary row and a final `big picture` cell were also applied directly; the cs notebook got an `addendum` cell with the same big picture. re-execute with `refresh_gv2_nb.py` (queue `jobs/queue/004_refresh_gv2_nb.sh`, defq, ~30 min; backup `.bak_<date>` next to it), never `build_gv2_nb.py`. the agent commits ipynb up to 40 mb.
- tofts: `results/tofts_vs_patlak/REPRO.md`.
- phantom nik: `xph_train.py` / `xph_eval.py`, `XPH_SIM=motion` for the motion sim. grasp v2 phantom: `xph_v2_sweep.py <G>` with `LAM_FRAC`. in vivo grasp v2: `grasp_v2/grasp_v2_real.py` (NLINE, LAM_FRAC, KEEP80).
